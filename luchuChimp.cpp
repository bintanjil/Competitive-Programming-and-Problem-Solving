#include <bits/stdc++.h>
using namespace std;
int main()
{
    int n,q,l=-1,r=-1;
    cin>>n>>q;
    int lad[n];
    int qui[q];
    for(int i=0;i<n;i++)
    {
        cin>>lad[i];
    }
     for(int i=0;i<q;i++)
    {
        cin>>qui[i];
    }
   int f=qui.begin();
   int l=qui.end();
    while(q--)
    {
     // int  mid=qui.begin()+qui.end();

    }
}
